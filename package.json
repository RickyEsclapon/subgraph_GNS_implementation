{
  "name": "gns-implementation-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gns-implementation-test",
    "create-local": "graph create --node http://localhost:8020/ gns-implementation-test",
    "remove-local": "graph remove --node http://localhost:8020/ gns-implementation-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gns-implementation-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
